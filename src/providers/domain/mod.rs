//! This context mostly implements traits owned by its consumers; domain
//! here is limited to provider-selection value objects shared by those
//! implementations (e.g. retry/backoff policy types).
