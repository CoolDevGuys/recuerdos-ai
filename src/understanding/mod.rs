//! Understanding context: the LLM pipeline — extract, reconcile, label.
//! Owns `Candidate`, `Reconciliation` (ADD/UPDATE/DELETE/NOOP), `Taxonomy`.

pub mod application;
pub mod domain;
pub mod infrastructure;
